package com.example.readmoretextview

object Constants {

    /** use number of lines as the limit. */
    const val LIMIT_MODE_LINES: Int = 0
    /** use number of words as the limit. */
    const val LIMIT_MODE_WORDS = 1

    /** default limit of lines. */
    const val DEFAULT_LIMIT_LINES = 2
    /** default limit of words. */
    const val DEFAULT_LIMIT_WORDS = 200

    /** when expanded, whether to show folded text choice or not. */
    const val SHOW_FOLDED_CHOICE = true

}